<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}Edinburgh Photographic Society</title>
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="description" content="{% if description %}{{ description }}{% else %}Edinburgh Photographic Society - Founded in 1861, welcomes all who share an interest in photography{% endif %}">
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body class="min-h-screen flex flex-col bg-gray-50 text-gray-800">
    <header class="bg-white shadow-md">
        {% include "partials/navigation.njk" %}
    </header>

    <main id="main" class="flex-grow page-container">
        {{ content | safe }}
    </main>

    <footer class="bg-gray-800 text-white py-8">
        {% include "partials/footer.njk" %}
    </footer>

    <script>
        if (window.netlifyIdentity) {
            window.netlifyIdentity.on("init", user => {
                if (!user) {
                    window.netlifyIdentity.on("login", () => {
                        document.location.href = "/admin/";
                    });
                }
            });
        }
    </script>
</body>
</html>